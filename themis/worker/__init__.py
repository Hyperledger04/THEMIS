# themis/worker/ — ARQ async job worker (V3.4 living agent runtime).
# main.py     — WorkerSettings + entrypoint (python -m themis.worker.main)
# jobs.py     — run_matter_job, index_matter_job, recover_paused_matters
