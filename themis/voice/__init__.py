# themis/voice — Voice AI gateway package.
# Provides Speech-to-Text (STT) and Text-to-Speech (TTS) adapters
# that bridge audio channels (browser WebSocket, Twilio phone) to the
# existing LangGraph pipeline without touching any node or graph logic.
