# indian_doc_formatter — Accumulated Rules

Updated after every formatting session where the user identifies a recurring
preference. New entries are appended — never overwrite existing entries.
Date-stamp every entry: [YYYY-MM-DD].

---

## Rules

<!-- Entries added here apply to EVERY formatting run unless overridden.
Example format:
[2026-01-15] Always use 12pt Times New Roman for shareholder agreements,
even if user pastes content in a different font.
-->

---

## Observations

<!-- One-off notes that may be promoted to Rules if they repeat.
Example format:
[2026-01-15] NDA for US counterparty used Schedule A/B/C instead of 1/2/3
— intentional, not a standing rule.
-->
